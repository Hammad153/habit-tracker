# React Native / Metro and Babel expect a flat node_modules layout. pnpm's
# default isolated layout hides transitive Babel plugins (e.g. babel-preset-expo's
# @babel/plugin-transform-react-jsx) from the resolver, breaking `expo export`
# in CI. A hoisted linker matches npm/yarn and is Expo's recommended setup.
node-linker=hoisted
